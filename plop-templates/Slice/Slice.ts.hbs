import { createSlice } from '@reduxjs/toolkit'

interface I{{pascalCase name}}State {
  value: null
}

const initialState: I{{pascalCase name}}State = {
  value: null
}

export const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState,
  reducers: {}
})

export const {} = {{camelCase name}}Slice.actions

export const {{camelCase name}}Selector = (state) => state.{{camelCase name}}.value

export default {{camelCase name}}Slice.reducer
